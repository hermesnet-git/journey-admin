package com.jouney.admin.domain.componentregistry;

/** Agrupamento por família de uso (independente do nível 0-3 do catálogo, que descreve
 * complexidade/camada) — usado pelo Form Builder pra organizar a paleta de componentes. */
public enum ComponentCategory {
    CONTENT,
    LAYOUT,
    INPUT,
    ACTION,
    FEEDBACK
}
