<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{correlationId}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--
        ELK/Logstash shipping: disabled, no ELK stack in this environment yet.
        To enable: add the `logstash-logback-encoder` dependency, add a JSON-encoded appender here
        (or a socket/TCP appender pointing at Logstash), and reference it from <root>/<logger> below.
    -->

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
